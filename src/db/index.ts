// Database layer is currently disabled.
// The app uses localStorage (via Zustand persist) for history.
// When you decide to add a real database, install drizzle-orm + mysql2 and implement this file properly.

export {};
